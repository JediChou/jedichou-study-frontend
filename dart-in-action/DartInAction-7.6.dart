// Dart in Action
// Listing 7.6 Calling toString() outputs a textual
// representation of the object

main() {
	print(1.toString());
	print("dart".toString());
	print(true.toString());
	print(null.toString());
	print(new Object().toString());
	print(1);
}
